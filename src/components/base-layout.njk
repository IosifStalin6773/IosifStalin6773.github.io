<!DOCTYPE html>
<html lang="{{ site.lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, viewport-fit=cover">
    <meta name="description" content="{{ description or site.description }}">
    <meta name="keywords" content="{{ keywords or site.keywords }}">
    <meta name="author" content="{{ author or site.author.name }}">
    <meta name="robots" content="index, follow">
    
    <!-- Open Graph -->
    <meta property="og:title" content="{{ title or site.title }}">
    <meta property="og:description" content="{{ description or site.description }}">
    <meta property="og:type" content="{{ og_type or 'website' }}">
    <meta property="og:url" content="{{ site.url }}{{ page.url }}">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ title or site.title }}">
    <meta name="twitter:description" content="{{ description or site.description }}">
    
    <title>{{ title or site.title }}</title>
    
    <!-- Security Headers -->
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; font-src 'self' https://cdnjs.cloudflare.com; img-src 'self' data: https:; connect-src 'self'; frame-ancestors 'none';">
    <meta http-equiv="X-Content-Type-Options" content="nosniff">
    <meta http-equiv="X-Frame-Options" content="DENY">
    <meta http-equiv="X-XSS-Protection" content="1; mode=block">
    <meta http-equiv="Referrer-Policy" content="strict-origin-when-cross-origin">
    <meta http-equiv="Permissions-Policy" content="geolocation=(), microphone=(), camera=()">
    
    <!-- HTTPS Enforcement -->
    <meta http-equiv="Strict-Transport-Security" content="max-age=31536000; includeSubDomains; preload">
    
    <!-- Preconnect to external domains for performance -->
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- DNS prefetch for external resources -->
    <link rel="dns-prefetch" href="//github.com">
    <link rel="dns-prefetch" href="//credly.com">
    <link rel="dns-prefetch" href="//mirrors.creativecommons.org">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" crossorigin="anonymous" media="print" onload="this.media='all'">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    
    {% if extra_css %}
        {% for css_file in extra_css %}
        <link rel="stylesheet" href="/css/{{ css_file }}">
        {% endfor %}
    {% endif %}
</head>
<body>
    {% include "../components/header.njk" %}
    
    <main>
        {{ content | safe }}
    </main>
    
    {% include "../components/footer.njk" %}
    
    <!-- Scripts -->
    <script src="/js/script.js"></script>
    <!-- Radical Scroll Fix - Minimal Approach -->
    <script>
        // Simple and radical scroll fix
        function enableScroll() {
            // Reset all scroll-related styles
            document.body.style.overflow = '';
            document.body.style.overflowX = 'hidden';
            document.body.style.overflowY = 'scroll';
            document.body.style.height = 'auto';
            document.body.style.position = 'relative';
            document.body.style.transform = 'none';
            document.body.style.webkitTransform = 'none';
            
            document.documentElement.style.overflow = '';
            document.documentElement.style.overflowX = 'hidden';
            document.documentElement.style.overflowY = 'scroll';
            document.documentElement.style.height = 'auto';
            document.documentElement.style.position = 'relative';
            
            // Enable touch scrolling
            document.body.style.webkitOverflowScrolling = 'touch';
            document.documentElement.style.webkitOverflowScrolling = 'touch';
            
            // Remove all touch event listeners that might block scroll
            const events = ['touchstart', 'touchmove', 'touchend', 'touchcancel'];
            events.forEach(eventType => {
                const listeners = document.getEventListeners ? document.getEventListeners(document)[eventType] : [];
                if (listeners) {
                    listeners.forEach(listener => {
                        document.removeEventListener(eventType, listener.listener);
                    });
                }
            });
            
            // Force viewport to allow scrolling
            const viewport = document.querySelector('meta[name="viewport"]');
            if (viewport) {
                viewport.setAttribute('content', 'width=device-width, initial-scale=1.0, user-scalable=yes');
            }
            
            // Remove any CSS that might block scroll
            const style = document.createElement('style');
            style.textContent = `
                * {
                    -webkit-overflow-scrolling: touch !important;
                    touch-action: auto !important;
                    transform: none !important;
                    -webkit-transform: none !important;
                }
                body, html {
                    overflow-x: hidden !important;
                    overflow-y: scroll !important;
                    height: auto !important;
                    position: relative !important;
                }
            `;
            document.head.appendChild(style);
        }
        
        // Apply immediately and on load
        enableScroll();
        document.addEventListener('DOMContentLoaded', enableScroll);
        document.addEventListener('load', enableScroll);
        
        // Reapply every second to ensure it stays working
        setInterval(enableScroll, 1000);
        
        // Reapply on resize/orientation change
        window.addEventListener('resize', enableScroll);
        window.addEventListener('orientationchange', enableScroll);
    </script>
    <!-- Mobile interactions completely disabled to fix scroll
    <script src="/js/mobile-interactions.js"></script>
    -->
    
    {% if extra_js %}
        {% for js_file in extra_js %}
        <script src="/js/{{ js_file }}"></script>
        {% endfor %}
    {% endif %}
    
    {% block scripts %}{% endblock %}
</body>
</html>
